apiVersion: v1
kind: Pod
metadata:
  name: hello-pod
  labels:
    zone: prod
    version: v1
spec:
  containers:
  - name: hello-pod
    image: nginx:1.25
    ports:
    - containerPort: 80
    resources:
      limits:
        memory: 256Mi
        cpu: "0.5"